@model List<clinicSystem.Models.Entities.Schedule>
@{
    ViewData["Title"] = "Doctor Schedules";
}

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />

<div class="container my-5 animate__animated animate__fadeIn">

    <div class="text-center mb-4">
        <h1 class="page-header">
            All <span class="text-primary">Doctor Schedules</span>
        </h1>
        <h3 class="page-subtitle">
            Here you can view all doctor schedules with start and end times
        </h3>
    </div>

    <div class="d-flex justify-content-end mb-4">
        <a class="btn btn-custom btn-lg shadow-sm animate__animated animate__fadeInDown" asp-action="New">
            <i class="bi bi-plus-circle"></i> Add Schedule
        </a>
    </div>

    <div class="table-responsive">
        <table class="table table-hover table-striped table-bordered align-middle shadow-sm animate__animated animate__fadeInUp">
            <thead class="table-light text-center">
                <tr>
                    <th>Doctor</th>
                    <th>DayOfWeek</th>
                    <th>StartTime</th>
                    <th>EndTime</th>
                </tr>
            </thead>
            <tbody class="text-center">
                @foreach (var item in Model)
                {
                    var start = DateTime.Today.Add(item.StartTime);
                    var end = DateTime.Today.Add(item.EndTime);

                    <tr class="table-row-hover">
                        <td class="fw-bold text-primary">@item.Doctor.Name</td>
                        <td>@item.DayOfWeek</td>
                        <td>@start.ToString("hh:mm tt")</td>
                        <td>@end.ToString("hh:mm tt")</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>

<style>
    .page-header {
        font-size: 3rem;
        font-weight: 700;
        color: #2c3e50;
        text-shadow: 2px 2px 4px rgba(0,0,0,0.2);
    }

        .page-header span {
            color: #0dcaf0;
        }

    .page-subtitle {
        font-size: 1.5rem;
        font-weight: 400;
        color: #7f8c8d;
    }

    /* ==== Buttons ==== */
    .btn-custom {
        border: 2px solid #0dcaf0;
        color: #0dcaf0;
        background-color: transparent;
        font-weight: 600;
        transition: all 0.3s ease;
        border-radius: 10px;
        font-size: 1.2rem;
        padding: 0.8rem 2.5rem;
    }

        .btn-custom:hover {
            background: linear-gradient(135deg, #0dcaf0, #0aa1c0);
            color: #fff;
            transform: scale(1.05);
        }

    /* ==== Table ==== */
    .table thead th {
        font-size: 1.2rem;
        letter-spacing: 0.5px;
        color: #2c3e50;
    }

    .table tbody td {
        font-size: 1.05rem;
        padding: 0.75rem;
    }

    .table-row-hover:hover {
        background-color: #e6f7ff !important;
        transition: background-color 0.3s ease;
        cursor: pointer;
    }

    a.text-primary:hover {
        color: #0d6efd;
        text-decoration: underline;
    }
</style>
